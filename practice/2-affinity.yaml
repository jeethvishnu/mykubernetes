apiVersion: v1
kind: Pod
metadata:
  name: preffered
spec:
  affinity:
    nodeAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 1
        preference:
          matchExpressions:
          - key: project
            operator: In
            values:
            - lock
  containers:
  - name: nginx
    image: nginx