apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: exp
  region: us-east-1

managedNodeGroups:
  - name: ondemand
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
